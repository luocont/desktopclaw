{
  "name": "frontend",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "start vite dev --host & wait-on http://localhost:5173 && electron .",
    "dev": "vite dev",
    "build": "vite build",
    "electron:build": "vite build && electron-builder"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "vite": "^5.0.0",
    "vite-plugin-electron": "^0.29.0",
    "wait-on": "^7.2.0"
  }
}
